# Copy to terraform.tfvars and fill in, then run the bootstrap once.
github_repository = "owner/repo" # owner/repo of THIS GitHub repository
aws_region        = "eu-west-1"
project_name      = "music-streaming-etl"
# state_bucket_name = "my-custom-tfstate-bucket" # optional; otherwise auto-derived
